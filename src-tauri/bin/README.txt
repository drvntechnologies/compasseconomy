Place SimConnect.dll here before building.

Copy it from your MSFS SDK installation:
  C:\MSFS SDK\SimConnect SDK\lib\SimConnect.dll

This DLL gets bundled with the production build so end users
don't need the MSFS SDK installed -- they only need MSFS itself.
